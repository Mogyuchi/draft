module.exports = {
	filters: {
		comments: true,
		'ja-named-entities': true,
	},
	rules: {
		// 技術文書向けpreset。
		// 設定値は一切変更せず、デフォルトの厳しさを維持する。
		'preset-ja-technical-writing': true,

		// Preset-japaneseにのみ含まれているルール。
		// 康煕部首など、見た目が漢字に似た異常なUnicode文字を検出する。
		'no-kangxi-radicals': true,

		// JTF日本語標準スタイルガイド。
		//
		// Technical-writingと重複・競合するルールや、
		// Markdownベースの技術ブログでは強すぎる表記ルールを無効化し、
		// 有用な追加チェックだけ利用する。
		'preset-jtf-style': {
			// ─────────────────────────────
			// 文体
			// Technical-writing側に任せる
			// ─────────────────────────────

			'1.1.1.本文': false,
			'1.1.2.見出し': false,
			'1.1.3.箇条書き': false,
			'1.1.5.図表のキャプション': false,

			// ─────────────────────────────
			// 句読点
			// Technical-writingの
			// Ja-no-mixed-period等に任せる
			// ─────────────────────────────

			'1.2.1.句点(。)と読点(、)': false,
			'1.2.2.ピリオド(.)とカンマ(,)': false,

			// ─────────────────────────────
			// 文字
			// ─────────────────────────────

			// 辞書ベースで誤検知しやすいため無効
			'2.1.2.漢字': false,

			// Technical-writingのno-hankaku-kanaと重なるうえ、
			// JTF側には辞書によるカタカナ表記統一も含まれる
			'2.1.5.カタカナ': false,

			// 「サーバー」「サーバ」などに強い方針を持ち込むため無効
			'2.1.6.カタカナの長音': false,

			// 全角数字 → 半角数字
			'2.1.8.算用数字': true,

			// 全角アルファベット → 半角アルファベット
			'2.1.9.アルファベット': true,

			// 0,5 のような誤った小数点表記など
			'2.1.10.算用数字の位取りの表記': true,

			// 辞書ベースで表記方針が強いため無効
			'2.2.1.ひらがなと漢字の使い分け': false,

			// Technical-writingのarabic-kanji-numbersが
			// このJTFルールそのものを利用しているため無効
			'2.2.2.算用数字と漢数字の使い分け': false,

			// 「3ヶ月」など助数詞の表記をチェック
			'2.2.3.一部の助数詞の表記': true,

			// ─────────────────────────────
			// スペース
			// ─────────────────────────────

			// 「TypeScript では」→「TypeScriptでは」のように
			// 日本語と半角英数字の間の余分な空白を禁止
			'3.1.1.全角文字と半角文字の間': true,

			// 日本語文字同士の不要なスペース
			'3.1.2.全角文字どうし': true,

			// カタカナ複合語の区切り方まで規定するため無効
			'3.2.カタカナ語間のスペースの有無': false,

			// 括弧の内外に不要なスペースを入れない
			'3.3.かっこ類と隣接する文字の間のスペースの有無': true,

			// ─────────────────────────────
			// 句読点・記号
			// ─────────────────────────────

			'4.1.1.句点(。)': true,
			'4.1.3.ピリオド(.)、カンマ(,)': true,

			// Technical-writingでは!?自体を禁止するためJTF側は不要
			'4.2.1.感嘆符(！)': false,
			'4.2.2.疑問符(？)': false,

			'4.2.4.中黒(・)': true,
			'4.2.5.波線(〜)': true,
			'4.2.6.ハイフン(-)': true,
			'4.2.7.コロン(：)': true,
			'4.2.8.セミコロン(;)': true,
			'4.2.9.ダッシュ(-)': true,

			// ─────────────────────────────
			// 括弧
			// ─────────────────────────────

			'4.3.1.丸かっこ（）': true,
			'4.3.2.大かっこ［］': true,
			'4.3.3.かぎかっこ「」': true,
			'4.3.4.二重かぎかっこ『』': true,
			'4.3.5.二重引用符': true,
			'4.3.6.中かっこ{ }': true,
			'4.3.7.山かっこ<>': true,
			'4.3.8.一重引用符': true,
		},
	},
};
